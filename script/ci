#!/bin/sh

set -e

script/bootstrap

COOKBOOKS_PATH=cookbooks

echo "  + Installing cookbooks and their dependencies."
rm -rf "$COOKBOOKS_PATH"
bundle exec berks install --path "$COOKBOOKS_PATH"

echo "  + Running RSpec tests."
exec bundle exec rspec --color --format documentation "$COOKBOOKS_PATH"
