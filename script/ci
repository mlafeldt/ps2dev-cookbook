#!/bin/sh

set -e

script/bootstrap

COOKBOOK_PATH=cookbooks

echo "  + Installing cookbook shims."
rm -rf $COOKBOOK_PATH
bundle exec berks install --shims $COOKBOOK_PATH

echo "  + Running tests."
exec bundle exec rspec --color --format documentation $COOKBOOK_PATH
