#!/bin/sh

set -e

script/bootstrap

COOKBOOKS_PATH=cookbooks

echo "  + Installing cookbook shims."
rm -rf "$COOKBOOKS_PATH"
bundle exec berks install --shims "$COOKBOOKS_PATH"

echo "  + Running tests."
exec bundle exec rspec --color --format documentation "$COOKBOOKS_PATH"
